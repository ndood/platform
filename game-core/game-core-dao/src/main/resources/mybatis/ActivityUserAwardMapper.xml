<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fulu.game.core.dao.ActivityUserAwardDao">
    <resultMap id="ActivityUserAwardResultMap" type="com.fulu.game.core.entity.ActivityUserAward">
                                    <id column="id"  property="id" />
                                            <result column="user_id"  property="userId" />
                                            <result column="activity_id"  property="activityId" />
                                            <result column="create_time"  property="createTime" />
                        </resultMap>

    <resultMap id="ActivityUserAwardResultVOMap" type="com.fulu.game.core.entity.vo.ActivityUserAwardVO" extends="ActivityUserAwardResultMap">
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,activity_id,create_time    </sql>

    <select id="findById" parameterType="java.lang.Integer" resultMap="ActivityUserAwardResultMap">
        select
        <include refid="Base_Column_List" />
        from t_activity_user_award
        <where>
        id = #{id}
        </where>
    </select>

    <select id="findAll"  resultMap="ActivityUserAwardResultMap">
        select
        <include refid="Base_Column_List" />
        from t_activity_user_award
    </select>

    <select id="findByParameter" resultMap="ActivityUserAwardResultMap" parameterType="com.fulu.game.core.entity.vo.ActivityUserAwardVO">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_activity_user_award
        <where>
                                                                            <if test="userId != null">
                       and user_id = #{userId}
                    </if>
                                                                <if test="activityId != null">
                       and activity_id = #{activityId}
                    </if>
                                                                <if test="createTime != null">
                       and create_time = #{createTime}
                    </if>
                                    </where>
    </select>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from t_activity_user_award
        <where>
            id = #{id}
        </where>
    </delete>

    <insert id="create" useGeneratedKeys="true" keyProperty="id" parameterType="com.fulu.game.core.entity.ActivityUserAward">
        insert into
        t_activity_user_award (user_id,activity_id,create_time)
        values
        (#{userId},#{activityId},#{createTime})
    </insert>

    <update id="update" parameterType="com.fulu.game.core.entity.ActivityUserAward">
        update t_activity_user_award
        <set>
                                                        
                    <if test="userId != null">
                        user_id = #{userId},
                    </if>
                                            
                    <if test="activityId != null">
                        activity_id = #{activityId},
                    </if>
                                            
                    <if test="createTime != null">
                        create_time = #{createTime},
                    </if>
                                    </set>
        <where>
            id = #{id}
        </where>
    </update>
</mapper>