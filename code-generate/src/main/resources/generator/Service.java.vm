package ${package}.service;

import ${package}.entity.${className};

import java.util.List;
import java.util.Map;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Service extends ICommonService<${className},${pk.attrType}>{
	
}
