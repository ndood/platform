package ${package}.dao;

import ${package}.entity.${className};
import ${package}.entity.vo.${className}VO;

import java.util.List;

import org.apache.ibatis.annotations.Mapper;

/**
 * ${comments}
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Mapper
public interface ${className}Dao extends ICommonDao<${className},${pk.attrType}>{

    List<${className}> findByParameter(${className}VO ${classname}VO);

}
